[phases.setup]
nixPkgs = ["python311", "playwright-driver"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "crawl4ai-setup",
  "playwright install chromium"
]

[start]
cmd = "gunicorn server:app --timeout 300 --workers 2 --bind 0.0.0.0:$PORT"
